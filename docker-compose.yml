services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: statice_mrf
      POSTGRES_USER: statice
      POSTGRES_PASSWORD: statice123
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data:
